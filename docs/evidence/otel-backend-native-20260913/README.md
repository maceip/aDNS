# Isolated native VM observability backend — 13 September 2026

The approved Collector → Tempo/Loki/Grafana backend was deployed on the existing Azure BIND VM as the separate `agentdns-otel-native-validation` Compose project. The protected Managed Run Command completed with actual execution `Succeeded`, exit 0; this result is distinct from management-plane provisioning success. The application checks finished at Unix 1789294151.7510254. This is a **synthetic backend transport/privacy proof on the Azure VM**, not a native CCF transaction or a replacement for the separately retained native DNS acceptance.

The actual deployment result records the four official AMD64 image references, container identities, published ports and resource limits. Each backend service has a 0.25 CPU cap and 128 PID limit; memory caps are 384/768/512/512 MiB for Collector/Tempo/Loki/Grafana. Docker 29.1.3 was retained. The standalone official Compose 5.1.2 binary was downloaded under the new observability directory and checked against the recorded release SHA256; the system Docker installation was not upgraded.

The authenticated receiver accepted TLS 1.3 using the fresh public test CA and the exact public-IP certificate identity. Missing and incorrect Basic credentials returned 401; the same certificate was rejected under the ordinary untrusted CA store. A synthetic span containing deliberate private-field canaries was accepted, retrieved by exact trace ID `9c11667c743d450cd4abed734436dc3a`, and checked for field removal. Grafana authenticated user, datasource, dashboard and proxied-trace API resources passed. Raw response bodies are retained under `verification/`; this is API evidence, not a rendered UI screenshot.

The sole new NSG rule permits TCP 4318 from the previously observed test-primary egress 20.82.173.102/32 to private IP 10.71.0.4. Both DNS 53 rules remained unchanged. Collector publishes only that private-IP listener; cleartext 4318 and gRPC 4317 are not published. Tempo 13200 and Grafana 13000 bind only loopback; Loki is unpublished. Any later egress-IP adjustment requires its own actual source observation and result, and is not implied by this snapshot.

BIND retained container 553c55cd4980…, PID 2475, process-start ticks 38227, restart count 0 and exact named.conf SHA256 1ed3e45c29b882fa164dc51d48c310064a4a7ccd15448c5c2bce200b7463e3a6. Both TCP and UDP answered authoritatively with SOA serial 34 immediately before and after installation. Earlier read-only preflight observed serial 32; normal autonomous signing continued. The first preflight attempted `dig` inside the minimal secondary image, which does not include it; that failure is retained. The corrected preflight used bounded host-side DNS wire probes and passed before any backend installation. Its post-install invocation deliberately skips the unused-backend-port test while preserving the exact BIND checks; published backend ports are checked independently.

`source/` contains exact executed public configurations and installer/verifier bytes, matched against the installation hash inventory. Actual runtime parameter files, server private key, CA private key, htpasswd and Grafana/exporter credentials are excluded. The CA private key never left the control host; only the server key/certificate and runtime authentication files entered the protected VM deployment. The public CA is included. The actual runtime path environment file is intentionally omitted even though its hash remains in the installation inventory.

The eight remote verification files were reconstructed from four bounded read-only chunks, with full-bundle and individual-file SHA256 checks, strict filename/size bounds, bounded decompression and exact comparison to the separately retrieved deployment result. The public export was checked against all actual backend credentials and private-key PEM markers before copying. All source and artifact hashes are listed in `sha256.json`.
