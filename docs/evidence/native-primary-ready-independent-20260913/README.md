The ACL-corrected native primary at4.208.82.123 passed this separate offline independent check at Unix time 1789287725. Its actual SNP report is **version3**, although the deployment directory retains the `native-v5-ready` release name. It matches the independently supplied CCE policy `8603aef5c7e35fb24ec32902fcad8572474e814f7b7f54abaa4b6e83ad50a94e`. [The report](independent-crypto.json) includes16 negative checks against this fresh capture; [seven guards](guard-tests.log) check the3/5 layout distinction and malformed input rejection.

The AMD P384/SHA384 signature verifies bytes0–671. Version3 has no mitigation-vector fields: bytes504–671 are reserved and zero. All four actual TCB states10/0/23/84 exactly meet the preapproved floor. The current AMD path to the pinned Genoa ARK, exact UVM measurement, Microsoft DID/feed and SVN104 verify. UVM publisher expiry is evaluated under the explicitly selected approved-release mode; strict current-certificate expiry rejects this same endorsement. The version3 layout was checked against [AMD Table23](https://docs.amd.com/api/khub/documents/x_tYkgJNtfpvqwPq45NrSQ/content); the distinct version5 layout is in [AMD ABI1.58 Table23](https://www.amd.com/content/dam/amd/en/documents/developer/56860.pdf).

[The supplied service CA verifies the captured node certificate](service-ca-relationship.json), whose SPKI digest is35db9f1e658e79159a42a36fb2d851a895c970c5c42460530b83bb22d356e6ed. Actual pinned CA retrieval is a separate bootstrap action; this offline verifier made no network requests. No worker COSE envelope was created, no trust setting changed, and this result does not assert service admission or DNS readiness. Tamper negatives retain the original AMD signature and are not freshly signed malformed hardware reports.

The previous v5-only helper first rejected this quote's version; that attempt is retained separately, and the offline helper then gained explicit version3 handling. Earlier hardware evidence bundles retain their original source and inputs. Included public sources and inputs reproduce this result using [the recorded command and immutable runtime](provenance.json). No credentials, private keys, deployment parameters or ledgers are included.
