Actual native CCF bootstrap on 2026-09-13, using the published SNP v5-capable image and the readiness-corrected configuration. The final allocated node produced report version3 under the preapproved Genoa/TCB/UVM profile; the preserved earlier node demonstrated version5 and motivated the narrow parser correction. No older hardware was selected and no policy floor was lowered.

The service CA was obtained only after verifying the fresh SNP quote against the prewritten CCE policy and matching the actual TLS peer key. Member activation, service opening and zone/policy/grant/TSIG-scope governance committed on that authenticated service. Automatic maintenance generated confidential DNSSEC keys and signed246base records; the actual KSK receipt was independently verified using that authenticated CA. The earlier denied-readiness failure is included separately.

This bundle covers bootstrap and public proof only. Read the native acceptance report for registration, lifecycle, external DNS and performance outcomes. Runtime parameters, TSIG/private signing keys, ledger files and raw logs remain private. The deployment uses ephemeral validation storage and is not evidence of production backup durability.
