The rebuilt native primary at20.166.239.227 passed this separate offline independent check at Unix time 1789286770. Its actual v5 quote and captured node P384 TLS key match the independently supplied CCE policy `bcda0e39dabca3347887d13902cd550f47e0785b4a969f7bc0981bd56c811d63`. [The report](independent-crypto.json) includes16 negative checks against this final capture. The earlier primary evidence remains in its separate bundle.

The genuine AMD P384/SHA384 report signature verifies bytes0–671, including launch/current mitigation vectors7/7. Reserved bytes520–671 are zero. All four TCB states10/0/27/88 satisfy the preapproved floor10/0/23/84; the pinned Genoa ARK chain, exact UVM measurement, Microsoft DID/feed and SVN104 verify. The UVM publisher certificate is expired now and is checked under the explicitly selected approved-release policy; the strict-current-expiry variant rejects it. See [AMD ABI1.58 Table23](https://www.amd.com/content/dam/amd/en/documents/developer/56860.pdf).

[The supplied service CA also verifies the captured node certificate](service-ca-relationship.json). Actual pinned CA retrieval is a separate bootstrap action; this offline verifier made no network requests. No worker COSE envelope was created, no trust setting changed, and this result does not assert service admission, DNS readiness or live application acceptance. Report tamper negatives retain the original AMD signature and are not freshly signed malformed hardware reports.

The included sources and public inputs reproduce the check using [the recorded command and immutable runtime](provenance.json). Six guards previously passed against these exact verifier source hashes; they were not rerun for this capture. This explicit public artifact inventory excludes credentials, private keys, deployment parameters and ledgers.
