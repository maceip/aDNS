# Native acceptance continuation

This document describes the execution sequence. Actual completed native registration and lifecycle checks are recorded in [the native API proof bundle](evidence/native-api-final-20260913/README.md) and [the native acceptance report](acceptance-native-final-20260913.md); each phase retains its measured inputs and results. The user separately approved image publication and then deployment, runtime test credentials, and the remaining attestation, registration, DNS and lifecycle acceptance. The current primary was pushed as `primary:20260913-native-v5` on September 13; independently fetched registry index and AMD64 manifest bytes match the pins below. Ordinary gzip layers and ordinary pulls are retained. The native run is in progress under `.validation/azure/native-final-20260913/`. Earlier approval conditions retained below describe the historical gate, which this explicit approval now satisfies. Local Virtual CCF and seeded lifecycle tests retain their separate labels.

Pin the CCF image and binary from [the final build record](evidence/ccf-native-v5-20260913/build.json), the current capture image from [its build record](evidence/aci-capture-deadline-image.json), and the exact generated CCE policies. The historical native capture used an older image; its public evidence cannot authenticate a replacement image, a new worker key, or new mail/lifecycle endpoints. Every replacement worker needs fresh capture, native appraisal under its matching policy, and equality between the actual TLS peer key and the appraised SPKI before a control token is sent. Authenticate the CCF service certificate using the node audit before trusting it for submission or receipt verification. Record whether the primary is Virtual or hardware-backed in every resulting acceptance summary.

The runtime image is pinned to [the SNP v5 build record](evidence/ccf-native-v5-20260913/build.json). Native deployment exposed two concrete compatibility gaps: the mounted configuration omitted SNP collateral and UVM attachments, and a genuine primary report used AMD SNP report version 5. The corrected bootstrap explicitly supplies the three UVM files and endorsed collateral servers. It also permits exact `/node/state` on the loopback interface for the supervisor readiness probe: CCF returns HTTP503 for an ACL-denied route, which prevented the driver from starting under the earlier bootstrap. Other node routes remain denied internally. The narrow parser update accepts the documented version 5 mitigation fields within the unchanged signed region and preserves CPUID/product checks. The current inputs are under `.validation/azure/native-final-20260913/primary/native-v5-ready/`; the historical [prepared contract bundle](evidence/azure-prepared-contract-20260913/summary.json) and provisional node evidence retain their original identities.

| Artifact | SHA-256 |
| --- | --- |
| Primary OCI index | `c921354c712a293af58925657404be3e28853cc70e652d164e4f793da3f2ed1a` |
| Primary AMD64 manifest | `4f1e45729cf74748f897cb64ec86c6be3e7e6fbdd711774c698222e9f38d2dff` |
| Primary executable | `6bcbf0a885cabdbea18ea3a25e0a3c56a49deaf1cf97e100ec97ff069ada8683` |
| Assembled constitution | `bc18a726e061fcb0b45c64fb4e67e774355c5c0abcfb772966bb9704ee129346` |
| Primary CCE policy | `8603aef5c7e35fb24ec32902fcad8572474e814f7b7f54abaa4b6e83ad50a94e` |
| Bootstrap manifest | `f4d501a1521ef98ea87135ce955fba638a733d7acd2236f52cf7cc475278838a` |
| Complete build-input manifest | `861f78453c8a4511afd26a8a9260dfd266f9e7084fd0b57012a74e25cf55b26e` |

The earlier prepared primary bundles, including the `eb62c060...` TTL candidate and its `bd43386e...` CCE, are superseded and must not be deployed. The new candidate adds bounded committed request observations, permanent TSIG identity revocation and immutable appraisal-policy identities. Regenerating any artifact requires review of its replacement pins.

The unchanged worker CCE hashes are `68f46918d210a8eb57ccbdade383ba459cb5260273be3d5f778ca37eef6ab75a` for the first new worker and `d8251696c7dcad3eaca72e3eb34b86dd28c936b056567e3a6ad5e6049024fd29` for the rotation worker. Both use capture manifest `sha256:61027bfc59833975356d526d58557e2b18e4af76464b52acb448e3a9a5feba7a`; their policies differ because their fixed configurations differ. The unchanged secondary manifest is `sha256:dfd9158f60a9a0cd67b445b001ee636a0389c66ce926af3c386588d886976861`.

The current reviewed deployment inputs are exported in [the native readiness bootstrap bundle](evidence/azure-native-v5-ready-20260913/README.md), with private run inputs at `.validation/azure/native-final-20260913/primary/native-v5-ready/ccf.template.json`, `preflight.json`, the separate mode 0600 `ccf.parameters.json`, and `control/`. The member and transfer secrets remain unchanged and are supplied only at runtime. The original public template and preflight in the prepared contract bundle are historical: Azure rejected their duplicate TCP/UDP port 53 declarations, and the first native CCF start identified the omitted attestation configuration. The corrected ACI group exposes UDP53, TCP8000 and TSIG-protected TCP5353. The separate stock BIND VM provides the standard TCP+UDP53 frontend. The final published single-image archive is `.validation/ccf-native-v5-build-20260913/primary-native-v5-image.tar`; its remote identity and complete-layer credential scans are included in the new build bundle. See [the native acceptance report](acceptance-native-final-20260913.md) for actual deployment and trust-check status.

Use two fresh constrained workers for overlap. Their configuration must name the same zone, mailbox domain, service host, addresses and approved ports, with distinct fixed request IDs and distinct key-bound grants. Allow the precise register, renew, deregister and challenge operations in each grant, with exact `zones`, `mailbox_domains`, `service_hosts`, `roles`, `address_cidrs`, sorted `ports`, and `acme_names` containing the configured service host. Each `subject_spki_sha256` must match that worker's freshly appraised key. Keep grant and appraisal-policy validity long enough for the planned lifecycle. Record actual peer certificates and independently verified native appraisal results for both workers. Neither private key leaves its worker.

Before admitting either worker, govern **one zone appraisal policy** using `adns_set_appraisal_policy` with `args: {"zone": "example.test.", "policy": POLICY_OBJECT}`. Its `approved_host_data` array must contain both independently reviewed worker CCE hashes, alongside the approved measurements, TCB/UVM requirements and current validity bounds described in [the policy schema](attestation-status.md). Use the same reviewed `policy_id` and complete policy for both worker appraisals and both admissions. The zone stores one policy row; changing its `policy_id` to add worker B invalidates worker A. Keep the node-bootstrap policy separate: it approves the primary CCE, not either workload. Any `approved_release` UVM time policy is an explicit reviewed policy choice. Set appraisal, grant and policy lifetimes to cover the planned idle window and terminal tests; do not silently accept an earlier clamped deadline.

The commands below use placeholders for reviewed local files and numeric addresses. For a new native ACI bootstrap, run `python3 tools/prepare_aci_control.py CONTROL_DIR --constitution-sha256 PACKAGED_CONSTITUTION_SHA256 --native-snp`; omit `--native-snp` only for the explicitly local Virtual runner. The ACI template helper and preflight reject a bootstrap without the native collateral configuration. Regenerate and independently review its CCE before observing a new quote. Create the mode-restricted output directory first. Build the offline verifiers with `cargo build --locked -p adns-attest --example appraise --example audit_ccf_node`. `python3 tools/audit_ccf_node.py --url https://NODE_IP:8000 --policy NODE_POLICY_JSON --output NODE_AUDIT_DIR` authenticates the service certificate into `NODE_AUDIT_DIR/service_cert.pem`; `python3 tools/fetch_capture.py WORKER_IP CAPTURE_DIR` retrieves only the worker public bootstrap. Before any token-bearing command, appraise that fresh capture under the joint workload policy. Governance commands use `python3 tools/ccf_control.py propose --url https://agentdns.test:8000 --connect-ip NODE_IP --service-cert SERVICE_CERT --member-key MEMBER_KEY --member-cert MEMBER_CERT --body REVIEWED_ACTIONS_JSON --output GOVERNANCE_RESULT_JSON`; the body is a JSON actions array, and acceptance requires its confirmed global-commit transaction ID.

For the first registration:

1. Read the fixed action and compare every field with the approved configuration and grant. Confirm the native evidence digest and signer SPKI match the saved capture. Send that exact action to `/app/service/nonce` on the authenticated CCF endpoint.
2. Require the nonce's intent hash to match the JCS action hash, and its committed response to contain an unexpired `expires_at`. Translate `expires_at` to `nonce_expires_at` for the worker. A previously prepared but unsigned expired nonce must be replaced before the first signature.
3. Request the signature with all mandatory inputs as shown below. The client appraises before loading the token, checks the actual pinned peer before POST, and saves the returned signed envelope with mode 0600.
4. Submit the exact envelope with the complete measurement command below, preserving the certificate name while connecting to the numeric Azure address. Require a successful committed body and matching committed header transaction ID, then an identical historical result from `/app/service/request?grant_id=...&request_id=...`. Derive `reg-` plus the first 32 hex characters of SHA256 over JCS of exactly `action`, `nonce`, `nonce_expires_at`, and `intent_hash`; compare it with the actual committed `registration_id`.
5. Immediately after A's admission, while its original nonce expiry is still in the future, run the consumed-nonce negative below before slower receipt, propagation or mail checks. It uses a separate sacrificial lifecycle action, not a retry of the successful registration.
6. Obtain the actual KSK receipt, verify its endorsement, Merkle proof, claims and transaction identity independently under the authenticated service certificate, and derive the DNSSEC trust anchor only from that verified KSK. Wait for authenticated secondary observation at `/app/zone/status?zone=example.test.` and query BIND directly. Use bounded five-second polling with a 600-second deadline for propagation checks; retain a failure if the deadline expires. Validate the expected address, MX and every port-specific TLSA RRset with stock DNSSEC tools; compare each TLSA digest with the native worker's actual SPKI.

```sh
python3 tools/sign_capture_request.py --ip WORKER_IP --port 8080 \
  --capture CAPTURE_DIR --policy JOINT_WORKLOAD_POLICY_JSON \
  --appraiser target/debug/examples/appraise \
  --token-parameters WORKER_PRIVATE_PARAMETERS_JSON --body NONCE_BODY_JSON \
  --path /signed-request --output SIGNED_ENVELOPE_JSON
python3 tools/measure_ccf_request.py --url https://agentdns.test:8000 \
  --connect-ip NODE_IP --cacert SERVICE_CERT --envelope SIGNED_ENVELOPE_JSON \
  --output COMMITTED_RESULT_JSON
```

Issue a nonce with `python3 tools/ccf_control.py request --url https://agentdns.test:8000 --connect-ip NODE_IP --service-cert SERVICE_CERT --method POST --path /app/service/nonce --body ACTION_WRAPPER_JSON --output NONCE_RESPONSE_JSON`. `ACTION_WRAPPER_JSON` is exactly `{"action": ACTION_OBJECT}`; the capture's `action.json` already has this wrapper. The control tool saves an HTTP/result wrapper: extract its `body` fields when constructing `NONCE_BODY_JSON`, which must contain only `nonce`, `nonce_expires_at` and `intent_hash`. Fetch the receipt with the same control request command using GET and `/app/governance/ksk-receipt?zone=example.test.`; extract its response `body` before running `python3 tools/verify_ksk_receipt.py RECEIPT_BODY_JSON --service-cert SERVICE_CERT --zone example.test.`.

For the consumed-nonce negative in step 5, prepare one constrained `renew` action through `/lifecycle/action` using a fresh dedicated request ID and a valid bounded `requested_lease_seconds`. Preserve the returned `action_id`, exact action and intent hash. Do **not** obtain a new CCF nonce for this negative. Call the normal `/lifecycle/signed-request` endpoint with that prepared `action_id`, A's already-consumed registration nonce and its unchanged original expiry, and the new renew action's actual `intent_hash`. The nonce's original registration binding is deliberately inappropriate for this new action; the signed intent itself must remain canonical and match the prepared renew action. This is a separately signed new request, not an altered historical retry.

Require the returned signature to verify under A's freshly appraised SPKI and submit the exact envelope to `/app/service/renew`. The current implementation returns HTTP403 with `INVALID_NONCE`, because successful admission removed the consumed nonce before this new request ID can execute. Check the actual API error as well as HTTP status; a worker-side refusal, bad signature, generic authentication failure or expired-nonce response is not this proof. Require unchanged registration/contributions/serial, an unchanged original successful reconciliation result, and no durable failure observation for the sacrificial request (`GET /service/request` returns HTTP404 because it has no live matching nonce or successful result). Account separately for any unrelated committed maintenance transaction.

Save the original capture/appraisal and TLS peer binding, original nonce response and successful registration envelope/result, prepared renew action, new signed envelope and actual rejection body, plus before/after committed state. Complete the negative before the original 300-second nonce deadline; do not extend that expiry or relabel an expired request as consumed-nonce proof. The worker signs this action once and will reject a replacement nonce for it. Retire this sacrificial request ID permanently after the negative, and use a fresh ID with a legitimately issued nonce for normal renewals. The user has approved these bounded worker calls as part of the isolated native acceptance run. The earlier approval hold is historical; actual signed requests and results are recorded separately.

Before the first admission in step 4, use the already signed native envelope for owner-scope negative checks. Govern one restriction at a time on its otherwise valid grant: remove the authorized host, role, address CIDR or port, then separately test expiration and revocation. Use `ccf_control.py request` on `/app/service/register` for these negative submissions, saving each response even though the tool exits nonzero on HTTP403. Require HTTP403 and no record/serial mutation; the final successful use of the same envelope establishes that none of these rejections consumed its nonce. For the expiration case, set both grant times to a valid ordered interval wholly in the past. Restore the full grant between cases. Keep the cases inside the five-minute nonce window and outside an unrelated scheduled signature refresh. Finally submit the identical envelope under the restored full scope and require one admission. This exercises an authentic native signature under unauthorized scope without asking the constrained worker to sign an arbitrary or out-of-scope action. Record governance transaction IDs and compare state, so scheduled maintenance is not misattributed to the denied request.

Use [the committed reconciliation contract](request-reconciliation.md) during both admission and negative checks. After nonce issuance, GET reports `pending` with `phase: awaiting_committed_result`; this is a committed observation of an issued intent, not evidence that a handler is running. Each valid signed scope rejection should produce an HTTP403 with committed observation metadata, and GET should report `failed` with the matching nonce and signed-message digest. The commit header confirms that diagnostic transaction only. Require the eventual `committed` execution result after restoring scope. Unknown or expired/pruned unsuccessful attempts can return HTTP404; a lost transport response alone never establishes failure.

Never change a nonce or request ID to resolve an uncertain submission. First reconcile the original grant/request identity and retry the exact saved envelope as needed. The constrained worker caches its signature and intentionally rejects a different nonce for an already-signed action. An uncommitted signed request that expires is an explicit acceptance failure to investigate; do not silently claim a second action is a retry. A globally committed historical result remains the same after its original nonce expires.

For each lifecycle action, call `/lifecycle/action` with only `operation`, a fresh request ID, and the documented bounded parameters. Check its returned fixed audience/grant/zone/signer and registration target. Issue the nonce for that action through CCF, then call `/lifecycle/signed-request` with `action_id`, `nonce`, `nonce_expires_at`, and `intent_hash`. Submit through the operation-specific CCF route and retain its committed result and exact historical reconciliation. CCF routes use POST for register/renew/deregister/challenge-create and DELETE for challenge-delete. A worker signature is never used as evidence of commitment.

Use the same complete `sign_capture_request.py` command for both lifecycle calls, replacing `--path`, `--body` and `--output`. These calls require the token and fresh native appraisal too. The `/lifecycle/action` body must have exactly `operation`, `request_id` and `parameters`; the worker inserts the registration ID and fixed scope itself:

| Operation | Required `parameters` sent to the worker |
| --- | --- |
| `renew` | `{"requested_lease_seconds": INTEGER}` |
| `deregister` partial | `{"selected_ports": [25], "withdraw_all": false}` with sorted configured ports; optional ASCII `reason` |
| `deregister` full | `{"selected_ports": [], "withdraw_all": true}`; optional ASCII `reason` |
| `acme_challenge_create` | `{"order_id": "UNIQUE_ID", "txt_value": "CANONICAL_43_CHARACTER_BASE64URL_SHA256", "ttl": 30, "lifetime_seconds": 120}` |
| `acme_challenge_delete` | `{"challenge_id": "chal-ISSUED_DIGEST_PREFIX"}` |

Wrap the returned `action` for CCF nonce issuance; do not send the `action_id`/`intent_hash` preparation wrapper as the action. The lifecycle signing body has exactly `action_id`, `nonce`, `nonce_expires_at` and `intent_hash`. `measure_ccf_request.py` selects the correct CCF method and route from the signed operation. Read current registration state at `/app/service/registration?registration_id=REGISTRATION_ID` to obtain `admitted_at` and actual validity bounds.

Renew while the registration is active. `requested_lease_seconds` measures duration from the registration's original `admitted_at`, not from the renewal request's time. The worker caps it at its configured initial lease, and CCF also bounds expiry by grant, policy and appraisal validity. Choose a duration that remains in the future and assert the actual formula; do not claim extension beyond the initial lease solely because a renew response succeeded. The worker attaches its evidence for reappraisal. Record the resulting appraisal/lease deadlines and verify any changed DNS state through the secondary.

After mail, overlap, ACME and the idle window, shorten the surviving registration with `requested_lease_seconds = current_real_time - admitted_at + 120`, using integer Unix seconds, provided the value is positive and at most the worker's configured `lease_seconds` (3600 for the prepared workers) and the grant's `max_lease_seconds`. Require grant, policy and fresh appraisal deadlines to exceed the target time, and verify the returned expiry equals that target; otherwise the observed expiry has another cause. Renewal itself does not change DNS serial; distinguish any concurrent automatic refresh from renewal. Then wait in real wall time for maintenance and secondary withdrawal. This places the natural lease deadline about two minutes ahead without changing the clock; record maintenance and secondary withdrawal latency separately, with at most 600 seconds of polling after that deadline. Challenge expiry can independently use a 60–120-second lifetime.

For overlapping keys, register worker B while A remains active. Wait for both expected TLSA values in each applicable port RRset, preserving the shared address and MX contributions. Use A's signed partial deregistration to remove only an explicit port; verify B's contribution and A's other ports remain. Use A's full withdrawal only after overlap, mail, ACME and idle checks, then verify the last applicable contribution determines whether each shared RRset remains. Run exact retries at the CCF boundary and retain the original transaction identity. The constrained worker refuses to sign changed parameters under an already-used request ID within one process, so that local refusal is not a CCF409 test. The real signed operator fixture exercises the common signature-verified result-conflict gate used by every operation. The bounded native restart sequence below adds a genuine native registration conflict through the existing API.

For ACME concurrency, prepare two distinct create actions with distinct order IDs and canonical SHA256 base64url TXT values. Compare each returned challenge ID with `chal-` plus the corresponding signed-message digest prefix. Confirm both values coexist in the DNSSEC-authenticated `_acme-challenge.<service-host>` TXT RRset. Delete one exact ID with its authorized worker; confirm the other value remains. Let the second challenge reach its bounded expiry without deleting it, and observe maintenance withdraw it. Keep a separately authorized active registration present for a targeted issuer-grant test, so disappearance can be attributed to the affected challenge contribution. For a targeted issuer test, remove only its `acme_names` authorization or challenge-create permission, preserving registration scope; restore it and create a fresh challenge for the next case. A changed zone policy ID, inactive profile or expired zone policy affects all registrations using that policy, so the unaffected control must be independently owned base/operator records or a separately governed zone. Do not claim that worker B under the same invalidated zone policy remains authorized.

Exercise expiry and revocation as distinct cases. Read registration state before maintenance to distinguish current eligibility (`status`, `active_ports`, `active_contributions`) from persisted DNS state (`committed_status`, `committed_contributions`), then wait for a new committed serial and authenticated secondary observation. Confirm relevant records disappear and independently owned records remain. Account for already cached positive and negative TTLs when observing clients. A short lease case and a separately revoked-grant case give clearer evidence than simultaneous expiry and revocation. Plan terminal cases explicitly: use A for partial/full withdrawal and B for natural lease expiry only after their nonterminal checks. Once maintenance persists withdrawal or expiry, restoring a grant does not reactivate that registration, and an exact register retry returns its historical result. The two constrained workers cannot mint replacement registration IDs. The twelve explicit acceptance checks require authentic expired/revoked-grant rejection before admission (check 2), natural lease/challenge expiry (check 7), and safe retirement during overlap (check 8). The two-worker sequence above covers these distinct lifecycle requirements. Native post-admission grant revocation and zone-policy withdrawal are additional coverage, already exercised by focused transaction tests; they must not be presented as part of this native run unless independently executed.

After worker A's full withdrawal has committed and propagated, retain its listener through the agreed cache/overlap interval, finish every A-dependent mail/rotation observation, and export its public evidence before stopping it. Then use this bounded sequence for native `REQUEST_ID_CONFLICT` coverage:

1. Restart A with its identical frozen capture image, configuration and CCE policy. Preserve the same fixed `grant_id` and registration `request_id`. This is a new process and must generate a fresh private key and native quote; no private key is restored or exported. The restart and subsequent token-bearing calls are covered by the user’s approved isolated native acceptance scope.
2. Fetch the replacement worker's fresh public capture. Independently verify its native evidence under the still-approved matching workload policy, its exact image/CCE, and its actual TLS peer SPKI. Require the key to differ from the original A key. Old capture files or the prior TLS pin do not authenticate this process.
3. Confirm original A is withdrawn and no active registration depends on its grant. Through committed governance, update only A's grant `subject_spki_sha256` to the freshly appraised key, preserving the reviewed scope and valid time interval. Worker B's separate grant and registration remain unaffected.
4. Read the replacement worker's fixed registration action. Its grant/request IDs are the original pair, while its signer and evidence digest differ. Obtain a fresh committed nonce for this exact action and request its normal `/signed-request` signature. The process has no prior signing cache, so this uses the existing constrained API without allowing arbitrary actions or a changed nonce for an already-signed action.
5. Submit that exact new envelope to `/app/service/register`; preserve the HTTP409 `REQUEST_ID_CONFLICT` result. Verify the original `(grant_id, request_id)` still reconciles to A's immutable successful result and original transaction ID, with no new registration or DNS/serial change attributable to the rejected request. The common conflict gate verifies the signature before comparing the stored signed-message digest, and returns before fresh admission/appraisal dispatch. Report the replacement evidence's independent native verification separately from the server's historical-result conflict decision.

Successful execution of this planned restart would prove that a changed, genuinely signed native registration conflicts without adding worker functionality, changing its image/configuration/CCE, or admitting another registration. It does not replace the successful original admission evidence or provide an identity for further terminal lifecycle tests. If B's natural expiry is still to run, leave B untouched and keep all checks inside its remaining validity window.

For optional additional native terminal coverage, use two fresh fixed configurations C and D only after A/B have completed their mandatory sequence. The existing image suffices; each distinct grant/request configuration requires its own reviewed CCE and fresh key/evidence. A new zone policy can approve C/D after A/B are inactive without interrupting them. Admit C and D, revoke only C's grant and observe its withdrawal while D remains, then select a new policy ID with otherwise identical valid policy content and observe D's policy-driven withdrawal while independently owned base/operator records remain. These are two separate terminal causes and require two newly admitted registrations; restoring a grant or policy cannot reactivate a withdrawn row. This optional extension can reuse the same two deployment slots sequentially, but needs explicit approval and preparation for the additional configurations. It is not a prerequisite added to the twelve mandatory checks.

Use the newer worker's real TLS mail listeners for controlled stock-client tests. Bind the actual SMTP STARTTLS/implicit TLS and IMAP peer keys to the published DNSSEC-validated TLSA. The fixture rejects message delivery and authentication; this demonstrates handshake and DANE/PKIX behavior only. A locally trusted fixture certificate is not public ACME issuance. A newly deployed image must be independently appraised before any token or protocol evidence is attributed to it.

Before terminal withdrawal/lease shortening, run the 1250-second idle refresh observer with 600-second signatures and a 300-second refresh interval, ordinary positive/negative DNSSEC probes, fixed offered query load, actual signing-span collection, and separate process RSS observation. Do not mutate the zone during that window. Retain complete outcomes, loss/failure counts, original signature expiry and observed serial progression. Public exports may contain receipts, service and worker certificates, signed public zone data, appraisal summaries, transaction IDs and diagnostics. Exclude control tokens, private keys, recovery shares, complete ledgers and secret configuration.
