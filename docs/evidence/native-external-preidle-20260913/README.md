The native primary and stock public BIND passed the initial external acceptance checks after both genuine worker registrations committed. [Summary](summary.json) and [DNS results](dns/results.json) record receipt-derived KSK/DS verification, a10-frame authenticated AXFR, whole-zone stock `ldns-verify-zone`, and13 positive/NSEC3 negative/wildcard `delv` checks. The transferred and served serials both equal9. Unsigned and wrong-key AXFRs were rejected, followed by another successful authenticated transfer. Both independently appraised worker SPKIs appear in the exact TLSA sets for25/465/993.

[Worker A](mail-a/native-mail-results.json) and [worker B](mail-b/native-mail-results.json) each passed stock Postfix DANE-only authentication against the actual native key. The wrong-key negative reached TLS and was rejected. Actual implicit TLS on465/993 passed controlled certificate/name validation and native-SPKI matching, with wrong-name and untrusted-certificate negatives. The client containers disclose DNAT from the signed reserved IPv4 address to each actual worker; DNS answers and TLS were not substituted. This does not prove reserved-address public routing, public certificate issuance or mail delivery.

[Operator policies](operator/exact-sets/results.json) verify complete SPF/DKIM/DMARC/TLSRPT/CAA RRsets, with a two-string long DKIM value. Apex TXT intentionally retains the governed base TXT alongside the operator contribution. ACME observations verify [both concurrent values](acme-both/results.json), [only the second after explicit deletion](acme-second-only/results.json), then [DNSSEC-proven absence after natural expiry](acme-expired/results.json). The second-only check completed before its deadline; absence was first observed41.986 seconds after expiry, so this is not an exact removal-latency measurement.

Included source and explicit public inputs are hashed. The source files were frozen before execution; input-integrity and bounded container-cleanup reports accompany the checks. Native node cryptography is documented separately in [the corrected-node bundle](../native-primary-ready-independent-20260913/README.md); this bundle does not replace the service admission/CCF transaction evidence or the subsequent sustained observation.
