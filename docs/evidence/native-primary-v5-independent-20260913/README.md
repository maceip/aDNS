This is an offline independent cross-check of the actual native primary CCF node quote and its captured TLS peer, verified at Unix time 1789285928. The supplied policy already contained the accepted CCE hash, measurement, TCB floor and UVM identity; the verifier did not derive trust settings from observed claims. [The report](independent-crypto.json) and [six layout/input guard results](guard-tests.log) passed. Sixteen negative checks changed public bytes or policy inputs and required rejection.

The AMD P384/SHA384 signature authenticates bytes 0–671, including the v5 launch/current mitigation vectors (both 7). Bytes 520–671 are zero as required by [AMD ABI 1.58 Table 23](https://www.amd.com/content/dam/amd/en/documents/developer/56860.pdf). The vectors are authenticated inventory; this policy has no mitigation-bit floor. All four actual TCB states are bootloader 10, TEE 0, SNP 27, microcode 88. The report binds the node P384 SPKI SHA256 to the first 32 report-data bytes, with zero padding in the remaining 32 bytes.

OpenSSL verified the current AMD certificate path against the pinned Genoa ARK key and the UVM publisher path under the explicitly approved immutable-release time policy. The UVM publisher certificate is expired at the check time; the strict-current-expiry negative therefore rejects this same authentic endorsement. No worker COSE wrapper was synthesized. This is neither service admission nor a fresh TLS handshake, and it makes no application readiness claim. Edited report negatives retain the original signature; they are not newly hardware-signed malformed reports.

The included source and public inputs reproduce the result with the command and immutable local runtime identified in [provenance.json](provenance.json). Synthetic byte layouts in the guard tests are only parser tests. The explicit public artifact inventory excludes runtime credentials, private keys, deployment parameters and ledgers.
