Rust and native C++ tracing validation on 2026-09-13. The workspace passed 116 tests across 19 executables, formatting, strict Clippy, and locked Rust 1.85.1 compilation. The native transport passed 14 cases including a 66-span request with a temporarily paused Unix receiver and 10,000 concurrent-producer requests with a permanently stalled receiver. The request loop completed in 11.5 ms; finite shutdown took 6.307 seconds. These are fixture measurements. CMake restored a missing generated CXX header in a fresh container. The subsequent real CCF quorum/recovery proof for exact ELF 6ba239f1dba3bc7f027b0857501c834b8049e5f6cf34f70844bbf2425299032f is in ../otel-ccf-live-20260913/. Image scanning checked all six runtime layers and 3,610 ordinary files against actual runtime-secret representations, plus embedded private-key blocks. No matches were found. No native hardware claim is made by these local tests.

The final v4 image retains the verified ELF and includes the package-import correction. Its exact identities, full-layer runtime-secret scan and SDK import probe are recorded in `final-image-review.json`. Root review also exercised the new ACI helpers with Ubuntu 24.04 system cryptography, reproduced an unavailable-date-property failure, and verified the compatibility correction with all 24 focused tests. Both the original failure and corrected output are retained.
