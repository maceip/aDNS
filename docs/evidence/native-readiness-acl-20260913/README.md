This isolated local test reproduces the native deployment readiness defect with the exact final CCF executable and packaged supervisor. [The old ACL](old-results.json) returns public `/node/state`200 but internal `/node/state`503 with an empty body, leaving the actual peering driver absent. [The corrected ACL](new-results.json) adds only `/node/state`, returns200, and starts the packaged `host_driver.py`. Internal `/node/network` remains503 in both cases.

Two disposable containers used the same immutable image, exact ELF, real pinned-bootstrap supervisor path, read-only public member/configuration inputs and ephemeral `/state`. A disclosed test-only launcher selects Virtual mode for the unchanged native ELF because the production supervisor intentionally strips that environment override. This is a local supervisor/ACL regression, not hardware evidence or completed Azure bootstrap. No running Azure node, production image, production code, or governed zone was changed by this test.

The explicit allowlist exports only source, public inputs, actual result records and invocations. Generated node private material and ledgers existed only in removed container tmpfs. Both containers terminated and were removed. The first diagnostic attempt assumed denied responses had JSON bodies; its failed logs remain separate, and the corrected raw-body reporter then passed both complete cases. See [summary and identities](summary.json).
