# Native stock BIND VM: runtime, transfer and memory evidence

The separate untrusted stock BIND VM at `20.166.33.141` served the genuine confidential ACI primary's signed zone over public TCP and UDP 53. See [the deployment report](../../acceptance-native-secondary-vm.md) for the exact image, runtime, process boundary and retained failures. Its confidential VM SKU is incidental: this secondary is not appraised and has no DNSSEC private key.

The public allowlist preserves the actual runtime inventory, initial SERVFAIL listener probes, failed first candidate readiness, guarded primary retarget, stock delv validation and BIND's signed NOTIFY/10-message AXFR log excerpt. It also retains the failed read-only-root `docker cp` staging attempt and successful separate 30-second collector smoke under their original source hashes.

The final memory collector ran for **1380.001697 seconds**, **08:32:05.219899–08:55:05.221595 UTC on 2026-09-13**, with **139 actual named PID1 samples**. The process start identity remained unchanged, the container restart count remained zero, and collection exited zero with an empty error log. Named RSS ranged from **54,684 to 54,752 KiB**; the maximum sample gap was **10.000106 seconds**. Separately, sampled cgroup charged memory ranged from **49,483,776 to 50,589,696 bytes**, with no OOM/limit event increments. Collector CPU was **0.175698 seconds**, collector maximum RSS **17,120 KiB**, and maximum counter-read duration **1.375 milliseconds**. Existing process/cgroup peak counters were not reset and are labeled as lifetime peaks; neither peak nor cgroup charged memory is substituted for sampled process RSS. This finite observation does not establish leak freedom.

The samples cover the independently recorded [1200-second external load](../native-external-idle-20260913/results/load/results.json), beginning **78.650 seconds before it** and ending **100.850 seconds after it**; 120 individual samples fall inside the load interval. The [analysis](memory/analysis.json) retains exact source hashes, wall-clock overlap and monotonic timing. The external load's packet loss and latency remain in its own complete report and are not filtered from this memory result.

All 139 raw samples, the exact collector/launcher/retriever sources, actual Azure responses and every chunk response are included. The exporter rechecked the four chunk hashes, bounded zlib reconstruction and complete 57,279-byte bundle SHA-256 `2cde6fc26f0de183ddabee331d719f2530072c732df2a312e1990eaff0ea6349` before copying. It rejected ten exact TSIG/private-key marker variants against the explicit selected files; runtime secrets, parameters, private SSH keys and ledgers are excluded. `sha256.json` covers all public artifacts in this bundle except itself.
