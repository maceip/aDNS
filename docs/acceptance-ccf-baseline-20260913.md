# CCF/BIND baseline before reconciliation and TSIG revocation changes

The complete isolated runner passed against executable `eb62c06083bdb7fe446f4d8cacd9629545e14ea049f1ebeaaf1c20ac59ef6d8e` from immutable image `sha256:cc749ceddf064ae31d4c571849d81f8d0a457ff135486b9020daa861f764b457`. This is the mixed-TTL-corrected **pre-reconciliation/revocation baseline**. Later request-observation and TSIG-governance changes require separate image-specific acceptance; this evidence is not relabeled for them.

The [74 public artifacts](evidence/ccf-runner-pre-reconciliation-revocation-20260913/sha256.json) came from the run's frozen allowlist exporter. All hashes were independently rechecked; no literal, base64 or hexadecimal value of its private TSIG secret appeared in the export. Sources, packaged binary and constitution hashes, image identities, actual requests and responses, raw logs and cleanup results remain recorded. The [complete result](evidence/ccf-runner-pre-reconciliation-revocation-20260913/runner-results.json) reports success, and cleanup removed every owned container and private CCF state volume.

## Real signature maintenance and transfer

External stock BIND was observed from **02:37:33 through 02:58:35 UTC on 2026-09-13**, a wall-clock span of **1,262 seconds**. Eighty-six independent positive/negative DNSSEC validations passed, crossing the initial signature expiration and observing serials 8–12. The committed-state observer recorded 126 successful, unexpired samples over 1,250.34 seconds, with a maximum sampling gap of 10.775 seconds. The 126 secondary-observation snapshots contained 122 `in_sync:true` values and four transient `false` values while CCF and BIND serials caught up; the external answers remained DNSSEC-valid. No operator mutation, restart, manual NOTIFY or work-queue reset occurred during this window.

The four automatic transfers to serials 9–12 reached BIND 296.707, 297.207, 296.680 and 297.034 seconds before the preceding signatures expired. These margins correlate the actual TSIG-authenticated transfer timestamps in the [BIND log](evidence/ccf-runner-pre-reconciliation-revocation-20260913/bind-container.log) with the preceding SOA signature expirations in the [external samples](evidence/ccf-runner-pre-reconciliation-revocation-20260913/frontend/remote-idle-samples.json). This is periodic external validation plus transfer/expiration evidence; it does not claim every possible query instant was probed.

The initial and final idle transfers each carried 978 records in ten TSIG-authenticated messages. The independent client verified every message, compared the transferred KSK to the independently verified CCF receipt, validated the entire zone with `ldns-verify-zone`, and rejected unsigned and wrong-key transfers. The fixture retained its 244 base records, including the apex TXT TTL 60 that exposed the earlier composition defect. Initial authenticated secondary observation required the normal bounded retry described in the [runner guide](acceptance-ccf-runner.md).

## Measured load and post-window mutation

The fixed DO=1 workload ran for 1,200.23 seconds at 1,000 offered qps, alongside ten sampled queries per second. All **1,199,997 of 1,199,997** load queries completed, with zero recorded losses and 999.997 completed qps. The separate probe recorded **12,001 successes**, zero failures, **p50 0.566 ms**, **p99 1.926 ms**, and maximum 22.138 ms. The [raw load and latency results](evidence/ccf-runner-pre-reconciliation-revocation-20260913/load/results.json) retain the measurement boundaries.

Each actual PID 1 process received 121 RSS observations over 1,200 seconds. CCF ranged **77,256–83,872 KiB**, starting at 80,584 and ending at 78,800 KiB. BIND ranged **38,344–40,492 KiB**, starting at 40,332 and ending at 39,588 KiB. RSS observations measure those processes only and do not establish the absence of memory leaks. The load ran on a shared development Docker host with amd64 CCF runtime emulation and other project work not excluded; it is a local baseline, not native Azure execution, dedicated-host capacity or WAN performance.

Seven actual `agentdns.dnssec.signing` spans in the [node log](evidence/ccf-runner-pre-reconciliation-revocation-20260913/node-container.log) ranged **138.281–173.408 ms**, with median **152.326 ms**. Six signed 244 source records; the post-window operator mutation signed 249. The span covers `SignedZone::sign_with_keys`, including denial/signature generation, and excludes surrounding storage and CCF consensus. Seven samples do not establish a stable tail-latency estimate.

After every observer finished, a real governed P256 operator request committed transaction **2.1800**, serial **13**. Its HTTP-to-confirmed-commit latency was 921.502 ms, including TLS. Exact retry preserved the original result, a changed signed action returned a conflict, and DER signatures failed without consuming the nonce. Stock BIND independently served exact SPF, DKIM, DMARC, TLSRPT and CAA bytes under valid DNSSEC, including the two-string DKIM value. The original base TXT TTL 60 and operator TXT TTL 300 remained unchanged.

This run uses the explicit local **Virtual CCF** test platform. It establishes real CCF commitment and stock-client protocol behavior in the stated setup. It does not establish native registration latency, native CCF execution, public delegation, public ACME issuance, attested-key lifecycle or mail delivery. Fresh native workload appraisal and cloud authorization retain their separate evidence and requirements.
